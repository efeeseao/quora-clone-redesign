import styled from 'styled-components';

export const Container = styled.section`
  > h1 {
    font-size: 2rem;
    font-weight: bold;
    color: #000;
  }

  > p {
    font-size: 1rem;
    color: var(--color-black);
  }

  > img {
    width: 100%;
    padding: 1rem;
  }
`;
